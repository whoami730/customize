# customize

Bash customization scripts - shell functions, git utilities etc

## Install

```bash
curl -fsSL https://whoami730.github.io/customize/install.sh | bash
```

Clones the repo to `~/customize` and sources it from `~/.bashrc`. Re-running is safe (pulls latest).

## Update

```bash
git -C ~/customize pull
```
